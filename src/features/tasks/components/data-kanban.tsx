"use client";

import { Task } from "@/features/tasks/types";

type Props = {
  data: Task[];
};

const DataKanban = ({ data }: Props) => {
  return <div>DataKanban</div>;
};

export default DataKanban;
